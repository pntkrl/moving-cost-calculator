{
  "name": "moving-cost-calculator",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/preload-fonts.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "wrangler pages deploy dist --project-name movecostmatrix --branch master"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@fontsource/inter": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.6",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "wrangler": "^4.108.0"
  }
}
